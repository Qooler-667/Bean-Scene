# VIZITKA

## 🛠 Технологии

- **Gulp 5** — сборщик задач.
- **BEM (БЭМ)** — методология верстки.
- **JavaScript (ES6+)** — логика и анимации.
- **BrowserSync** — сервер с автообновлением.
- **Плагины Gulp:** Autoprefixer, CleanCSS, Terser (сжатие JS), FileInclude (сборка HTML), WebP & Imagemin (оптимизация графики).

## 🚀 Возможности сборки

- Автоматическая конвертация шрифтов в **woff2**.
- Создание современных форматов изображений (**WebP**).
- Генерация **SVG-спрайтов** (все иконки в одном файле).
- Модульная структура HTML через `@@include`.
- Минификация CSS и JS для финальной версии (папка `docs`).

## ⌨️ Команды

- `npm install` — установить все зависимости.
- `gulp` — запустить проект в режиме разработки (слежение за файлами + сервер).
- `gulp cleanDist` — принудительная очистка папки сборки.
